// import { reqTest } from '@/api';
const state = {};

const mutations = {};

const actions = {
  // async Test(newId) {
  //   let res = await reqTest(newId);
  //   console.log(res);
  // }
};
const getters = {};
export default {
  state,
  mutations,
  actions,
  getters
};
